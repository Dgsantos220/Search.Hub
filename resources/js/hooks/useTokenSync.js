// Hook removido - autenticação agora é gerenciada via sessão/cookies do Laravel
// Este arquivo está mantido apenas para compatibilidade com imports existentes

export function useTokenSync() {
    // Não faz mais nada - sessão é gerenciada pelo Laravel
}
